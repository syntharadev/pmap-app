# ==============================================================================
# PMAP.APP - REQUIREMENTS CORE (AVX-Safe & API Integration) v2.2
# ==============================================================================

# 1. API Y SERVIDOR WEB (Capa de Ingesta de Datos)
fastapi>=0.111.0
uvicorn[standard]>=0.30.1
python-multipart>=0.0.9

# 2. ANÁLISIS FORENSE ESTRUCTURAL (CST / AST)
# Utiliza el módulo nativo 'ast' de Python 3.11+.

# 3. ANÁLISIS MATEMÁTICO, GRAFOS Y PERSISTENCIA (Seguro para NUC 8GB RAM)
networkx>=3.3
pydantic>=2.7.4
scikit-learn>=1.0.0
duckdb>=1.0.0

# 4. EXTRACCIÓN FORENSE ASISTIDA (Nuevo SDK Gemini 2026)
google-genai>=0.1.1

# 5. LICENCIAS Y SEGURIDAD LOCAL (JWT Soberano)
PyJWT>=2.8.0

# 6. ANÁLISIS CST / Tree-sitter (Detección de Amnesia Estructural)
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0

# 7. HERRAMIENTAS DE DESARROLLO Y TESTING
pytest>=8.2.2

# 8. HUB DE IDENTIDAD Y LICENCIAS (Supabase — Solo lectura, ping de validación)
supabase>=2.0.0